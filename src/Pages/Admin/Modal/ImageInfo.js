import React from 'react';
import styled from 'styled-components/macro';

const ImageInfo = () => {
  return <ImageContainer>여기 사진자리임</ImageContainer>;
};

const ImageContainer = styled.div`
  margin: 30px 0;
  width: 420px;
  height: 500px;
  background-color: yellow;
`;

export default ImageInfo;
